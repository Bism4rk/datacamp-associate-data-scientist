# Import numpy as np
import numpy as np

# Set the seed
np.random.seed(123)

# Generate and print random float
print(np.random.rand())

'''
O código acima demonstra como gerar um número aleatório em ponto flutuante entre 0 e 1 usando a biblioteca NumPy em Python. Ele também define uma semente para o gerador de números aleatórios, garantindo que o número gerado seja o mesmo toda vez que o código for executado.
'''