from things import life_exp
import matplotlib.pyplot as plt

# Create histogram of life_exp data
plt.hist(life_exp)

# Display histogram
plt.show()

'''
O código acima demonstra como criar e exibir um histograma usando a biblioteca Matplotlib em Python. Ele importa uma lista de expectativas de vida (life_exp) do módulo things.py, cria um histograma dessa lista e, em seguida, exibe o histograma em uma janela gráfica. Um histograma é uma representação gráfica que organiza um conjunto de dados em intervalos (bins) e mostra a frequência de dados em cada intervalo, permitindo visualizar a distribuição dos dados de forma clara.
'''